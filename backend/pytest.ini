[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    unit: Pure unit tests (no database or HTTP).
    service: Service-layer tests with mocked or in-process dependencies.
    integration: Tests requiring live API, database, or external services.
